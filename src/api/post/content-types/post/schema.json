{
  "kind": "collectionType",
  "collectionName": "posts",
  "info": {
    "singularName": "post",
    "pluralName": "posts",
    "displayName": "Post",
    "description": "Blog posts migrated from WP \"post\" type (flat blog — no categories/tags)"
  },
  "options": {
    "draftAndPublish": true
  },
  "attributes": {
    "wp_id": {
      "type": "integer",
      "required": true,
      "unique": true
    },
    "title": {
      "type": "string",
      "required": true
    },
    "slug": {
      "type": "string"
    },
    "excerpt": {
      "type": "text"
    },
    "body": {
      "type": "richtext"
    },
    "featuredImage": {
      "type": "media",
      "multiple": false,
      "allowedTypes": [
        "images"
      ]
    },
    "publishDate": {
      "type": "datetime"
    },
    "needsReview": {
      "type": "boolean",
      "default": false
    },
    "reviewNote": {
      "type": "text"
    },
    "seo": {
      "type": "component",
      "repeatable": false,
      "component": "shared.seo"
    },
    "tldr_answer": {
      "type": "text"
    },
    "action_log_refs": {
      "type": "json"
    },
    "author": {
      "type": "relation",
      "relation": "manyToOne",
      "target": "api::author.author"
    },
    "reviewer": {
      "type": "relation",
      "relation": "manyToOne",
      "target": "api::author.author",
      "description": "Named reviewer (master doc s20). Set only when a genuine review happened - never backfilled onto legacy content"
    },
    "reviewed_date": {
      "type": "date",
      "description": "Renders 'Reviewed {date}' only when a reviewer is set"
    },
    "headline_figure": {
      "type": "string",
      "maxLength": 40,
      "required": false,
      "description": "Large stat shown on research-hub cards (package 2 item 8), e.g. \"$6,659\""
    },
    "content_type": {
      "type": "string",
      "required": false,
      "description": "Editorial type driving the research-hub grids: data_story | research | guide. Absent from the original brief but its grids query it; plain string, team-populated."
    },
    "cluster": {
      "type": "string",
      "required": false,
      "description": "Topic cluster; the research hub separates cluster=provincial data stories into their own grid."
    }
  }
}